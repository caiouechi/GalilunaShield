<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>GalilunaShield.Web</RootNamespace>
    <AssemblyName>GalilunaShield.Web</AssemblyName>
    <Description>Galiluna Shield parent dashboard - a local web app the parent opens from their phone on the home network.</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="System.Security.Cryptography.ProtectedData" Version="8.0.0" />
  </ItemGroup>

</Project>
