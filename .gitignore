bin/
obj/
.vs/
*.user
# Whisper model files are downloaded on first run (~150 MB+)
models/
# WiX build output
installer/out/
publish/

# WiX extension cache
.wix/
# rendered screenshots are kept; scratch images are not
*.tmp
